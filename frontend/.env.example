# URL base de la API Django. En local, el backend corre en :8000 por defecto.
# Usa "localhost" (no "127.0.0.1"): la cookie de sesión es SameSite=Lax en
# desarrollo y solo viaja entre orígenes del mismo sitio.
VITE_API_BASE_URL=http://localhost:8000
